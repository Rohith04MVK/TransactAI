[project]
name = "transactai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
	"chromadb",
	"fastapi",
	"uvicorn[standard]",
	"pydantic",
]
